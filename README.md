# Learning Nix

## Basic nix

In `default.nix`.

Commands:

- `nix-shell`
- `nix-build`

Writes to simlink in `./result`

## Flake

In `flake.nix`

Commands:

- `nix build`
- `nix run`
- `nix develop`
